# CreativeMode
To download the mod only take the "CreativeMode" folder and put it into the plugins folder from bephinex

# Controls
press tab to toggle the UI and pause/unpuase the game, everything else will make sence in usage
